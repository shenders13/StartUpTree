<%= form_for(@stage) do |f| %>
  <% if @stage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stage.errors.count, "error") %> prohibited this stage from being saved:</h2>

      <ul>
      <% @stage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :stage_name %><br>
    <%= f.text_field :stage_name %>
  </div>
  <div class="field">
    <%= f.label :stage_description %><br>
    <%= f.text_field :stage_description %>
  </div>
  <div class="field">
    <%= f.label :stage_icon %><br>
    <%= f.text_field :stage_icon %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
